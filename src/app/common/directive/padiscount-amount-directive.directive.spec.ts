import { PaDiscountAmountDirective } from './padiscount-amount-directive.directive';

describe('PaDiscountAmountDirective', () => {
  it('should create an instance', () => {
    const directive = new PaDiscountAmountDirective();
    expect(directive).toBeTruthy();
  });
});
