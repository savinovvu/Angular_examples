import { PaDisplayValueDirective } from './pa-display-value.directive';

describe('PaDisplayValueDirective', () => {
  it('should create an instance', () => {
    const directive = new PaDisplayValueDirective(null);
    expect(directive).toBeTruthy();
  });
});
