import { PadiscountAmountDirectiveDirective } from './padiscount-amount-directive.directive';

describe('PadiscountAmountDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PadiscountAmountDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
