import { PaCategoryFilterPipe } from './pa-category-filter.pipe';

describe('PaCategoryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PaCategoryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
