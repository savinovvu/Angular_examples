import { PaDiscountPipe } from './pa-discount.pipe';

describe('PaDiscountPipe', () => {
  it('create an instance', () => {
    const pipe = new PaDiscountPipe();
    expect(pipe).toBeTruthy();
  });
});
